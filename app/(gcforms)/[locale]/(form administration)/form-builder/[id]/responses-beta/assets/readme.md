(le français suit)

## What you’ll get:

- A folder with raw JSON data files (these eliminate the need for a confirmation step).
- A CSV (spreadsheet) file that can have data appended to it as you get more responses.
- A folder with individual HTML copies of responses for processing submissions one at a time.
- A folder with file attached separate from answers.

---

## Ce que vous recevrez :

- Un dossier avec les fichiers de données brutes JSON (qui éliminent la nécessité d'une étape de confirmation).
- Un fichier CSV (feuille de calcul) auquel nous pourrons ajouter des données à mesure que vous recevez davantage de réponses.
- Un dossier avec les copies HTML individuelles des réponses pour traiter les soumissions une par une.
- Un dossier contenant les fichiers joints séparément des réponses.