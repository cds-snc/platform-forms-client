// import { checkFlag } from "@formBuilder/actions";

export const allowGrouping = async () => {
  // return checkFlag("conditionalLogic");

  // TEMP: for testing, do not merge to develop
  return true;
};
