{% extends "base.njk" %}

{% block content %}

    <h1>{{ __('[[sample]].title') }}</h1>

    <div>
        <p>
            {{ __('[[sample]].intro') }}
        </p>

        <form novalidate method="post">
            <input type="hidden" name="_csrf" value="{{ csrfToken }}">
            
            {{ textInput('form.firstname', 'form.firstname.desc') }}
            {{ formButtons() }}
        </form>
    </div>

{% endblock %}
