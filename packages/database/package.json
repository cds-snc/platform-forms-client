{
  "name": "@gcforms/database",
  "version": "1.0.0",
  "author": "Canadian Digital Service",
  "license": "MIT",
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "packageManager": "yarn@4.10.3",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "import": "./dist/index.js",
        "default": "./dist/index.js"
      }
    }
  },
  "scripts": {
    "reset": "prisma migrate reset -f --skip-seed",
    "db:generate": "prisma generate",
    "db:studio": "prisma studio",
    "db:prod": "prisma migrate deploy && tsx src/seed_cli.ts --environment=production",
    "db:dev": "prisma migrate dev && prisma db seed",
    "db:test": "prisma migrate reset --force && tsx src/seed_cli.ts --environment=test",
    "prebuild": "yarn clean && prisma generate",
    "build": "tsdown src/index.ts --format cjs,esm --dts --clean --splitting",
    "prepack": "yarn build",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^6.17.1",
    "@prisma/client": "7",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/node": "^24.7.1",
    "@types/pg": "^8",
    "prisma": "7",
    "ts-command-line-args": "^2.5.1",
    "tsdown": "^0.15.7",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
