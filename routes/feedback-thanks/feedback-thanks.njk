{% extends "base.njk" %}

{% block content %}

    <h1>{{ __('feedback-thanks.title') }}</h1>

    <div>
        <p>
            {{ __('feedback-thanks.text') }}
        </p>
        <p>
            {{ __('feedback.go_back') }}
        </p>
    </div>

{% endblock %}
