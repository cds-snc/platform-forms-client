/* istanbul ignore file */

const Schema = {
  oas: {
    isIn: {
      errorMessage: 'errors.multipleChoiceGeneric',
      options: [['oas', 'allowance', 'survivor', 'no']],
    },
  },
}

module.exports = {
  Schema,
}
