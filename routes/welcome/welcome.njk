{% extends "base.njk" %}

{% block content %}
    <h1>{{ __('welcome.title') }}</h1>

    <div class="bg-red-100 border-l-4 border-red-800 p-4 mt-8 hidden cookie-warning">
        <h2 class="text-2xl">{{ __('start.enable_cookies_title') }}</h2>
        <p class="p-0">{{ __('start.enable_cookies_body') }}</p>
    </div>

    
    <div class="">

        <div class="mb-20">
            <h2 class="text-3xl mb-5">{{ __('welcome.product.title') }}</h2>
            <p>{{ __('welcome.product.text') }}</p>
            <p>
                <a href="{{ __('welcome.intakelink.url') }}">{{ __('welcome.intakelink.title') }}</a>
            </p>
            <p>
                <a href="{{ __('welcome.ssclink.url') }}">{{ __('welcome.ssclink.title') }}</a>
            </p>            
        </div>

        <div class="mb-20">
            <h2 class="text-3xl mb-5">{{ __('welcome.design.title') }}</h2>
            <p>{{ __('welcome.design.text') }}</p>
            <p>
                <a href="{{ __('welcome.designsystem.url') }}">{{ __('welcome.designsystem.title') }}</a>
            </p>
        </div>

        <div class="mb-20">
            <h2 class="text-3xl mb-5">{{ __('welcome.technology.title') }}</h2>
            <p>{{ __('welcome.technology.text') }}</p>
            <p>
                <a href="{{ __('welcome.technologygit.url') }}">{{ __('welcome.technologygit.title') }}</a>
            </p>
        </div>

    </div>

{% endblock %}