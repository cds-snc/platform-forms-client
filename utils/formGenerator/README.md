# FORM GENERATOR

## STEPS

1. Copy '.env.example' to '.env'
`cp utils/formGenerator/.env.example utils/formGenerator/.env`

2. Update the DB 'DATABASE_URL' to whats in your '.env.yaml'

3. Install and run it
`cd utils/formGenerator && yarn generate`

4. Re-run dev
`yarn dev`
